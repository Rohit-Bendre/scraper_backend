{
  "build": {
    "command": "apt-get update && apt-get install -y openjdk-17-jdk && export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64 && export PATH=$JAVA_HOME/bin:$PATH && ./mvnw clean package -DskipTests"
  },
  "start": "java -jar target/*.jar"
}
